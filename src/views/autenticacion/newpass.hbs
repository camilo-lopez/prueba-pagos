<div class="container p-4">
    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="card text-center">
                <div class="card-header">
                    <h3>Nueva contraseña</h3>
                </div>
                <div class="card-body">
                    <form id="form" action="" method="POST"> 
                    <div class="form-row">

                        <div class="col p-4">
                            <input type="password" class="form-control" placeholder="Nueva contraseña" name="contrasena" id="contrasena" />
                        </div>
                         <div class="col p-4">
                            <input type="password" class="form-control" placeholder="Repetir contraseña" name="contrasenarep" id="contrasenarep" />
                            <label id="siono"></label>
                        </div>
                    
                        
                    </div>
                    <div class="form-group">
                        <button class="btn btn-success btn-block" disabled="disabled" id="btncambiar" > Cambiar contraseña </button>    
                    </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
<script>
   $( document ).ready(() => {
       var url = $(location).attr('pathname');
       var id = url.slice(21);
       console.log(id)
     $('#form').attr('action', `/forgot_p4ssw0rd/new/${id}`);
})
 function coincidePassword() {
    const val1 = $("#contrasena").val();
    const val2 = $("#contrasenarep").val();
    if(val1 != val2){

    }else{
        $("btncambiar").removeAttr("disabled")
    }
}

$("#contrasenarep").keyup(() => {
    const val1 = $("#contrasena").val();
    if(val1 == $("#contrasenarep").val()){
        $("#btncambiar").prop('disabled',false)
        $("#siono").text("Contraseña coincide"); 
         $("#siono").css("color","green"); 
    }else{
        $("#btncambiar").prop("disabled",true)
        $("#siono").text("Contraseña no coincide"); 
        $("#siono").css("color","red"); 
        
    }
    //coincidePassword();
})

</script>